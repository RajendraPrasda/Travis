os: linux
dist: focal

vm:
  create:
    name: my_customone_name  # Custom identifier string, human readable

addons:
  apt:
    update: true

before_install:
  # Prevent Travis from auto timeout due to no output
  - while true; do echo "💡 Build still running..."; sleep 60; done &
  - sudo apt-get -y install libxml2-dev

script:
  - echo "Custom image setup complete. Proceeding with build tasks."
  - sleep 5  # Replace this with your actual build step
