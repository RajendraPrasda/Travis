os: linux
dist: focal

jobs:
  include:
    - name: "One Job"
      env:
        - CUSTOM_IMAGE_NAME=custom_one_job
      vm:
        create:
          name: custom_one_job
      before_install:
        - echo "Starting custom image build: $CUSTOM_IMAGE_NAME"
      script:
        - echo "Build logic for $CUSTOM_IMAGE_NAME"
