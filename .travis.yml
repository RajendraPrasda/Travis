os: linux
dist: focal

jobs:
  include:
    - name: "First Job"
      vm:
        create:
          name: custom_first_job
before_install:
  - sudo apt-get -y install libxml2-dev

addons:
  apt:
    update: true

script:
  - echo "TEST"
