os: linux
dist: focal
jobs:
  include:
    - name: job onetest
      vm:
        create:
          name: custom_job_onetest_o
      script: echo "condition with job onetest"
before_install:
  - sudo apt-get -y install libxml2-dev
addons:
  apt:
    update: true
script:
  - echo "GITLAB TESTING"
