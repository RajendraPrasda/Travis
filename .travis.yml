os: linux
dist: focal

jobs:
  include:
    - name: "job one"
      vm:
        create:
          name: custom_job_one_o
      script: echo "condition with job one"

    - name: "job two"
      vm:
        create:
          name: custom_job_two_t
      script: echo "condition with job two"

    - name: "job three"
      vm:
        create:
          name: custom_job_three_th
      script: echo "condition with job three"
