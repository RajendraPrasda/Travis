arch: s390x
os: linux
dist: focal

vm:
  create:
    name: my_custom_name  # custom identifier string, human readable

addons:
  apt:
    update: true

before_install:
  - sudo apt-get -y install libxml2-dev

addons:
  apt:
    update: true
