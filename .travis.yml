os: #unsupported
dist: focal

vm:#existing key
  create:
    name: my_dbfail_name  # custom identifier string, human-readable

addons:
  apt:
    update: true

before_install:
  - sudo apt-get -y install libxml2-dev
